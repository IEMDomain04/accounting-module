import React from 'react'

const Dropdown = ({options}) => {
  return (
    <div>
      <select name="" id="">
        <option value=""></option>
      </select>
    </div>
  )
}

export default Dropdown
